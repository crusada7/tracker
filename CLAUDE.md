Проект: трекер сертификатов.
Один самодостаточный HTML-файл, публикуется через GitHub Pages
по адресу crusada7.github.io/tracker

Правила работы:
- Всё держим в одном index.html, не разбивать на файлы
- Внешние библиотеки разрешены (решение от 06.09.2026), но по умолчанию
  обходимся без них: файл должен открываться и работать офлайн, в том числе
  с домашнего экрана. Сейчас единственный внешний ресурс — шрифты Google Fonts
- Прогресс хранится в localStorage, не ломать эту логику
- Объясняй мне каждое изменение в коде построчно — я учусь читать код
- Перед пушем показывай, что именно изменилось
- Не хвали авансом, говори прямо, если что-то сделано плохо
